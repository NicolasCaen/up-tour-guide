# up-tour-guidé

## Description
Plugin WordPress permettant de créer des visites guidées (back-office, éditeur Gutenberg et front) en s’appuyant sur Driver.js.
Les visites sont définies via des templates XML activables/désactivables depuis l’administration.

## Installation
1. Copier le dossier `up-tour-guide` dans `wp-content/plugins/`.
2. Activer le plugin « Tour guidé » dans l'administration WordPress.
3. Aller dans le menu « Visites guidées » pour configurer les templates existants ou en importer de nouveaux (XML).
4. Pour chaque template, choisir sa disponibilité :
   - **Désactivé** : visite complètement désactivée
   - **Menu + URL** : visible dans le menu ET accessible par lien direct
   - **URL uniquement** : masquée du menu, accessible uniquement par lien direct (`?visite=ID`)
5. Dans l'éditeur Gutenberg, ouvrir le panneau « Visite guidée » pour lancer une visite.
6. Côté front ou admin, ajouter `?visite=ID_TEMPLATE` à l'URL pour lancer une visite spécifique (l'ID est défini dans l'attribut `id=""` du template XML).

## Changelog
- 2025-11-27 · v0.1.16.0 · **Type de template "Lien" et Menu Front** : Ajout d'un nouveau type de template `link` (sans étapes, juste URL et titre) permettant de créer des entrées de menu personnalisées (liens externes ou internes). Nouveaux statuts de disponibilité : `menu_front` (visible uniquement dans la barre admin côté front) et `menu_front_url` (visible en admin et front). Intégration de la barre d'admin "Visites guidées" côté front pour les utilisateurs connectés, respectant ces nouveaux filtres de visibilité.
- 2025-11-27 · v0.1.15.0 · **Médias pour les descriptions** : Ajout de sélecteurs de vidéos et d’images au-dessus du champ Description dans l’éditeur d’étapes, permettant de copier rapidement le code HTML `<video>` ou `<img>` correspondant (avec URLs relatives). Prise en charge des médias présents à la fois dans le dossier du plugin (`assets/media`) et dans un nouveau dossier d’uploads (`uploads/tour-guide/media`). Nouveau formulaire d’import de médias (images/vidéos) vers ce dossier, avec filtrage des types autorisés.
- 2025-11-26 · v0.1.14.0 · **Modules et Tri des visites** : Introduction de la notion de "Module" (brique réutilisable via `<include>`) distincte des "Visites" classiques. Les modules sont listés dans un tableau séparé avec leur ID visible et sont exclus du menu principal (statut forcé à URL uniquement). Le tableau "Mes Visites" est désormais triable par glisser-déposer, ce qui met à jour l'ordre d'affichage dans le menu d'administration. Amélioration de l'interface de gestion avec séparation claire des tables.
- 2025-11-26 · v0.1.13.0 · **Gestion des templates et Démarrage sur page** : Ajout de la fonctionnalité d'export XML (bouton "Exporter") pour tous les templates. Ajout de la suppression (bouton "Supprimer") pour les templates uploadés. Séparation claire des templates Plugin (lecture seule) et Uploadés dans l'interface. Disponibilité par défaut à "Menu + URL" pour les nouveaux templates. Ajout du champ `page_start` pour rediriger vers une URL spécifique avant de lancer la visite (ex: `/admin.php?visite=ID`).
- 2025-11-18 · v0.1.12.0 · **Éditeur de templates – types d’étapes et includes** : réorganisation du tableau d’édition (sélecteur CSS en première colonne), ajout d’un sélecteur de type d’étape (Popup / Action / Include) avec masquage dynamique des champs selon le type, et support visuel des balises `<include>` dans l’UI (étapes d’inclusion avec simple champ ID de template).
- 2025-11-18 · v0.1.11.0 · **Inclusion de templates** : nouvelle balise `<include template="ID" />` permettant de réutiliser les étapes d'un template existant dans un autre template, avec protection contre les boucles infinies. Idéal pour composer des workflows complexes en combinant des séquences communes (ex: combiner "ajouter-block" et "ajouter-composition" dans un seul tour). Documentation complète ajoutée dans `ORCHESTRATION.md` avec exemples d'utilisation. Nouveau template d'exemple `workflow-complet.xml` démontrant la composition de templates.
- 2025-11-18 · v0.1.10.0 · **Tours asynchrones et clics React** : implémentation complète des tours asynchrones Driver.js avec `onNextClick` dans `step.popover` pour gérer les actions de clic sur les éléments React/Gutenberg. Les clics ne se déclenchent que lors de l'appui sur "Suivant" (plus de double clic). Vérification intelligente des panneaux déjà ouverts pour éviter de les refermer. Orchestration adaptée : les actions `click` avec `resume="none"` ne s'exécutent plus dans la phase initiale mais uniquement via `onNextClick`. Simulation complète des événements souris (mousedown/mouseup/click) pour compatibilité React. Les templates `ajouter-composition` et `ajouter-block` fonctionnent désormais parfaitement avec ouverture/fermeture fiable des panneaux.
- 2025-11-18 · v0.1.9.0 · **Correction Driver.js dans Gutenberg** : correction majeure de l'appel `driver.drive()` pour l'éditeur Gutenberg (anciennement appelé avec `stepIndex` objet, maintenant sans options pour index 0). Suppression du wrapper HTML node qui cassait l'affichage des popovers. Ajout de la dépendance `wp-data` pour le bon fonctionnement de React hooks. Orchestration simplifiée et stabilisée. Les visites guidées s'affichent désormais correctement dans l'éditeur avec overlay et popovers visibles. Documentation détaillée de l'orchestrateur XML ajoutée (`ORCHESTRATION.md`).
- 2025-11-17 · v0.1.8.0 · Système de disponibilité à 3 états : remplacement de la simple activation/désactivation par un système granulaire (Désactivé / Menu + URL / URL uniquement). Permet de gérer finement la visibilité des visites dans le menu admin tout en gardant la possibilité de les lancer via un lien direct `?visite=ID`. Filtrage intelligent du dropdown admin (seules les visites "Menu + URL" apparaissent). Support de migration automatique depuis l'ancien format. Ordre personnalisable par drag & drop respecté dans le menu et l'API.
- 2025-11-17 · v0.1.7.0 · Amélioration des templates XML et de l'éditeur: les descriptions sont maintenant encapsulées en CDATA et auto-formatées en paragraphes `<p>…</p>` à partir des retours à la ligne (tout en respectant le HTML inline). Le bouton « Tester » dans l'éditeur applique exactement la même logique que la sauvegarde XML pour le rendu des paragraphes. En admin, meilleure gestion de la fermeture des popovers (croix visible + bouton natif Driver réactivé) sans casser le positionnement des visites.
- 2025-11-14 · v0.1.6.1 · Stabilisation de la fermeture des visites: prise en charge robuste des boutons Fermer/Terminer (fallback global), nettoyage complet de l'état Driver.js (classes `driver-active`/`driver-fade`, éléments overlay/popover), réouverture possible de l'UI après un tour. Ouverture fiable des sous-menus WordPress pour les étapes ciblant des éléments du menu (détection via l'élément réel fourni par Driver.js et scope `#adminmenu`).
- 2025-11-14 · v0.1.5.0 · Suppression orchestrateur: retour au mode natif Driver.js simple et stable. Menu dropdown custom unifié (plus de double affichage survol/clic), décodage HTML des titres (apostrophes), lien "Gérer les templates" intégré. Navigation Précédent/Suivant native et fonctionnelle.
- 2025-11-14 · v0.1.4.0 · Refonte UI éditeur de templates: layout en cartes ultra-compact, drag & drop par poignée, actions par étape (Tester/Dupliquer/Supprimer), masquage dynamique des champs d'action, sections "Action" et "Attendre" en lignes combinées, tooltips FR. Corrections admin bar et orchestrateur (attente réduite, fallback highlight), améliorations UX.
- 2025-11-14 · v0.1.3.0 · Listes de visites par template (admin bar et panneau Gutenberg), lancement individuel, compat Driver.js (ancienne/nouvelle API), passage à un bundle local (assets/lib), libellés FR (Précédent/Suivant/Fermer/Terminer), correctifs de chargement (éditeur/admin/front).
- 2025-11-14 · v0.1.1.0 · Ajout du contexte (admin/éditeur/front) aux templates, générateur de templates enrichi (formulaire + ajout d’étapes) et corrections Gutenberg.
- 2025-11-14 · v0.1.0 · Création du plugin, intégration Driver.js, templates XML, panneau Gutenberg et barre d’admin.
